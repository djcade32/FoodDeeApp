import { StyleSheet } from "react-native";

export default StyleSheet.create({
  loadingText: {
    marginTop: 10,
    textAlign: "center",
    color: "#CCCCCC",
    fontSize: 18,
    fontFamily: "lato",
  },
});
